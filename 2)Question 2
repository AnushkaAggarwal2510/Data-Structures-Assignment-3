Question 2) Given a string, reverse it using STACK. For example “DataStructure” should be output as “erutcurtSataD.” 

Answer 2)

#include <iostream>
#include <cstring>
using namespace std;

#define MAX 100

int main() {
    char str[MAX], stack[MAX];
    int top = -1;

    cout << "Enter a string: ";
    cin >> str;

    int length = strlen(str);

    for (int i=0; i<length; i++) {
        stack[++top] = str[i];
    }
    cout << "Reversed string: ";
    while (top != -1) {
        cout << stack[top--];
    }
    cout << endl;
    return 0;
}
